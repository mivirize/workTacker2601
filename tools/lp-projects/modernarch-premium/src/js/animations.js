document.addEventListener("DOMContentLoaded",()=>{const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible")}})},{threshold:0.1,rootMargin:"0px 0px -50px 0px"});document.querySelectorAll(".reveal-text,.award-item,.team-card,.image-reveal").forEach(el=>observer.observe(el));const splitTexts=document.querySelectorAll(".split-text");splitTexts.forEach(el=>{const text=el.textContent;el.innerHTML=text.split("").map((char,i)=>`<span style="transition-delay:${i*0.03}s">${char}</span>`).join("");observer.observe(el)});const tiltCards=document.querySelectorAll(".tilt-card");tiltCards.forEach(card=>{card.addEventListener("mousemove",(e)=>{const rect=card.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;const centerX=rect.width/2;const centerY=rect.height/2;const rotateX=(y-centerY)/10;const rotateY=(centerX-x)/10;card.style.setProperty("--rotateX",rotateX+"deg");card.style.setProperty("--rotateY",rotateY+"deg")});card.addEventListener("mouseleave",()=>{card.style.setProperty("--rotateX","0");card.style.setProperty("--rotateY","0")})})})
